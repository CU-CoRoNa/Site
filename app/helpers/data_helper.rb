module DataHelper
  #get a list of the results names but not the entire result i.e just name
  #load a few on the page
  #as the user scrolls down iterate through the array and request the full result
  #to be rendered
end
